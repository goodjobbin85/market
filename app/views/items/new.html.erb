<h2>Create New Item</h2>

<%= form_for(@item) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %></br>

	<%= f.label :price %>
	<%= f.text_field :price %></br>

	<%= f.label :quantity %>
	<%= f.text_field :quantity %></br>

	<%= f.submit %>

<% end %>