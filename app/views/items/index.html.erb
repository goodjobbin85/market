

<ul id="list">
<% @items.each do |item| %>
	<li><%= link_to item.name, item_path(item) %></li>
<% end %>
</ul>


<%= link_to "Add New Item", new_item_path, class: "alert alert-success add-button" %></br> 
	
