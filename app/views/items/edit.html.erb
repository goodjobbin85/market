<h1>Edit: <%= @item.name %></h1>

<%= form_for(@item) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %></br>

	<%= f.label :price %>
	<%= f.text_field :price %></br>

	<%= f.label :quantity %>
	<%= f.text_field :quantity %></br>

	<%= f.submit %>

<% end %>
